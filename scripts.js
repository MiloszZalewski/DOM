var withButtonClass = document.getElementsByClassName('button');
var buttonLength = buttonClass.length;
console.log(withButtonClass);

for (var i = 0; i <= buttonLength; i++ ) {
	console.log(document.withButtonClass[i].innerText);
}